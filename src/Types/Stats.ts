export interface Stats {
    speed: number,
    maxEnergy: number,
    metabolism: number,
    size: number,
    maxAge: number,
    maxHealth: number,
    damage: number,
    vision: number,
    aggression: number,
}